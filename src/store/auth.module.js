/* import the modules */

/* set the state if needed */
const state = {};

/* want to access state? use the getter! */
const getters = {};

/* make side effect or just function? use the actions */
const actions = {};

/* want to change value on state? use mutations */
const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations
};